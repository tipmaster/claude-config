make sure to get all recent code changes committed and synch them through git on the server
never copy code directly using scp
ssh -p 55000 root@nue; if you font know the working directory on the server ask
git does not need credetnials